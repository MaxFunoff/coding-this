query Post($id: Int!) {
  post(id: $id) {
    id
    title
    createdAt
    updatedAt
    likes
    voteStatus
    starStatus
    tags
    description
    creator {
      id
      displayname
    }
  }
}
