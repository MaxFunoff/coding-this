mutation Register($email: String!, $displayname: String!, $password: String!) {
    register(options: { email: $email, displayname: $displayname, password: $password }) {
      errors {
        ...UserError
      }
      user {
        ...RegularUser
      }
    }
  }